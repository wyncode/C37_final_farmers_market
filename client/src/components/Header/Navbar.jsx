import React from 'react';
import { AppContext } from '../../context/AppContext';

const Navbar = () => {
  const { Home } = useContect(AppContext);
  return (
    <Navbar>
      <Link></Link>
    </Navbar>
  );
};

export default Navbar;
